[package]
name = "projects_backend_database"
version = "0.1.0"
edition = "2024"

[dependencies]
api = { path = "./api" }
clap = { version = "4.5.23", features = ["derive", "cargo"] }
rocket = { version = "0.5.1" }

[workspace]
members = ["api", "domain", "infrastructure", "application", "shared"]
